# Both you and Saqib must use the SAME backend URL (not localhost)
# Quick test: use a tunnel URL while one person runs the server
# Permanent: use your hosted server URL (e.g. Render)
VITE_BACKEND_URL=https://your-shared-server-url
